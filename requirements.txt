# LiveKit Agents 1.1.7 核心库
livekit-agents==1.1.7

# 插件依赖
livekit-plugins-deepgram>=0.7,<2.0
livekit-plugins-groq>=0.1.1,<2.0
livekit-plugins-cartesia>=0.4.0,<2.0
livekit-plugins-silero>=0.7,<1.0

# 其他必要依赖
python-dotenv==1.0.0
asyncio 

# HTTP客户端 - 解决session问题
+ aiohttp>=3.10,<4.0
